# AGENTS.md

个人知识库构建项目。AI 在此仓库的职责是**写/改文档**，不是跑代码。本文件是 AI 代理接手任务前的入口。

## 启动协议

接到任何任务前，按顺序读取：

1. [`CONTEXT.md`](./CONTEXT.md) —— 项目上下文、目录结构、写作约定、当前进度（必读）。只用于结构与进度，不用于回答新问题
2. 写/改文档时读 [`01-AI行动规范/文档生成规范.md`](./01-AI行动规范/文档生成规范.md) —— 目标态只写对象与做法；每份文档只写该对象
3. 若要写/改某板块文档，先读该板块**已存在的同类文档**，保持风格一致并避免重复
4. 知识性新问题先按问题本身讨论，不读已有知识文档（见工作规则 8）

## 边界

**必做**
- 中文写作，代码与术语保留原文
- 新建/删除文件前向用户确认
- 任务完成后更新 CONTEXT.md 的"当前进度"与"待办"；进度只记阶段结果，不记每次措辞微调

**先问**
- 新建子目录或调整目录结构
- 修改 AGENTS.md / CONTEXT.md 的核心结构（非进度段）
- 引入新的写作约定或命名规则

**永不做**
- 删除或清空 CONTEXT.md 的"当前进度"段
- 在 02-专业知识/ 下创建与现有领域并列的新顶级领域目录
- 使用英文写正文（代码、术语、文件名除外）

## 目录导航

| 路径 | 内容 |
|------|------|
| `CONTEXT.md` | 项目上下文（必读） |
| `01-AI行动规范/` | 通用方法论：项目构建、维护、AI 编程、AI 调研 |
| `02-专业知识/` | 领域知识：当前含 Agent |
| `03-手动记录/` | 个人消化后写下的知识笔记 |
| `04-速查/` | 回头要查的事实（API、工具、链接等） |
| `专题/` | 按主题混放：知识、资料、原始文件可共存 |

## 工作规则

### 1. 听指令，不擅自行动
- Must 按用户指令逐步推进，不扩展任务范围
- Must 遇到歧义先提问，不允许猜测后继续
- Never 主动推进未授权的下一步（如"顺便补充相关文档"）

### 2. 先思考，再写文档
- Must 动手前说明：写什么、为什么、有什么权衡
- Never 进行无声假设——推不出结论时必须显式提问
- Must 有更简单方法时主动提出异议

### 3. 简洁至上
- Must 用最少的文字解决问题
- Never 添加推测性内容、为一次性段落创建结构
- Never 加无关的示例、注释、扩展说明（除非用户要求）

### 4. 外科手术式修改
- Must 只动必须动的地方
- Never "改进"相邻段落、措辞或格式
- Must 匹配现有风格

### 5. 主动获取最新信息
- Must 当涉及最新规范、工具版本等时效性信息时，主动联网搜索
- Never 仅凭训练知识回答可能已过时的问题

### 6. 知识性问题先讨论再落笔
- Must 用户提出知识性问题时，先就问题本身讨论清楚，不急于询问是否落笔或写文档
- Must 讨论清楚、且会影响怎么做/怎么生成时，直接写进对应文档，不等用户再说「写进去」

### 7. 文档生成、修改规范
写/改文档时遵守 [`01-AI行动规范/文档生成规范.md`](./01-AI行动规范/文档生成规范.md)。目标态只写对象与做法；每份文档只写该对象；过程态才写为什么、不选什么。

### 8. 新问题不套已有文档
- Must 用户提出新知识、新问题时，就该问题独立讨论与总结
- Never 为「保持一致」或「避免重复」去检索、引用、套用已有文档的分类、术语或结论来回答新问题
- Must 仅在用户明确要求对照已有内容，或任务已是写/改某板块时，才读该板块已有文档；读的目的是对齐风格与去重，不是改写问题本身的结论

## 踩坑记录

- **新问题被已有笔记带偏**：问新知识时不要先读已有文档再拼接回答。见工作规则 8。

